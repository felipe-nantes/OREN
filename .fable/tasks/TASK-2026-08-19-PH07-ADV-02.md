# TASK-2026-08-19-PH07-ADV-02 — PHASE_07, wave 2: mutação seletiva + loaders de robustness

STATUS: DONE (2026-08-19) - evidencia em TASK-2026-08-19-PH07-ADV-02_EVIDENCE.md
Autorização: Felipe Nantes, 2026-08-19 ("autorizo" sobre a proposta registrada em
TASK-2026-08-18-PH07-ADV-01_EVIDENCE.md, seção "Próxima wave proposta").

## Objetivo

1. Mutação seletiva nos braços fail-closed do núcleo científico (gap ledger
   G4-G8): splits.py, segmentation_contract.py, volumetry.py, robustness.py
   (_percentile/clinical_subtype_map). Saída: ledger de mutantes
   (KILLED/SURVIVED) com justificativa por sobrevivente.
2. G1/G2: testes com fixtures sintéticas para `_json`/`_jsonl`/
   `load_frozen_oof_predictions`/`evaluate_robustness` (robustness.py) — sem
   dados reais, sem tocar produção.

## Método

- Tentar mutmut 3.7.0 (compat Windows UNKNOWN — smoke test rápido; se falhar,
  documentar e usar fallback).
- Fallback validado (PHASE_04): sonda dirigida — escrever mutante → rodar
  testes relevantes → restaurar → VERIFICAR restauração via
  `git hash-object` == `git rev-parse HEAD:<file>`.
- Testes novos de G1/G2 e eventuais testes negativos diretos (G6) entram na
  suíte; mutantes NUNCA permanecem no working tree.

## Proibições

- Não alterar código de produção (sobreviventes vão para o ledger, correções
  ficam para PHASE_08/09 conforme risco).
- Não enfraquecer/remover testes existentes.
- Não commitar/pushar sem solicitação explícita.
- Restauração de cada mutante comprovada por hash antes da sonda seguinte.

## Critérios de saída

- Ledger de mutantes cobrindo os braços de erro dos alvos G4-G8, cada um
  KILLED ou SURVIVED+justificado.
- Testes G1/G2 passando; caminho de erro de splits (G6) afirmado diretamente
  se sondas revelarem lacuna.
- Evidence package com CONTEXT_EFFICIENCY.
