# TASK-2026-08-19-PH08-REF-01 — PHASE_08, wave 1: correções LOW do handoff adversarial

STATUS: DONE (2026-08-19) - evidencia em TASK-2026-08-19-PH08-REF-01_EVIDENCE.md
Autorização da fase: Felipe Nantes, 2026-08-19 ("commit e push, depois inicie a
PHASE_08"). Regra da fase (LONG_PLAN): pequenas melhorias, patch
behavior-preserving comprovado; suíte verde antes/depois de cada mudança.

## Escopo (insumos do exit review da PHASE_07)

1. **TD-015** — persist concorrente do estado de job pode falhar com WinError 5
   (os.replace), hoje apenas logado + tmp vazado. Fix: retry curto +
   try/finally para limpar o temporário. Teste dedicado.
2. **TD-007** — `_atomic_text` sem try/finally: tmp vazado em falha de
   os.replace. Fix: try/finally. Teste dedicado se ainda não houver.
3. **mypy robustness.py:226/233** — `sorted()` sobre chave `str | None`
   (TypeError latente se subtipo None entrar). Fix mínimo com saída idêntica
   para entradas todas-str (provado pela suíte + teste novo do caso None).
4. **7×B023** — closures em loop nos 3 scripts one-shot: vincular via
   argumento default. Comportamento idêntico (invocação intra-iteração já
   verificada na PHASE_07); ruff B023 zerado como prova.

## Fora do escopo desta wave

- Triagem/autofix do baseline ruff de estilo (~900) — wave 2, proposta à parte.
- Mudanças semânticas (direction-blind etc.) — PHASE_09 (HG-03).
- S5/S6 testes opcionais — só se sobrar orçamento e sem tocar produção.

## Proibições

- Nenhuma mudança de semântica científica (thresholds, denominadores,
  geometria, seleção DICOM) — qualquer necessidade dessas = STOP + gate.
- Não commitar/pushar sem solicitação explícita.

## Critérios de saída

- Cada item: diff mínimo + teste + suíte-alvo verde; suíte completa verde ao
  final da wave (exceto falha ambiental conhecida).
- mypy sem o achado 226/233; ruff B023 = 0.
- Evidence package com CONTEXT_EFFICIENCY.
