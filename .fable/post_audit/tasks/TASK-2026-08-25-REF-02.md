# TASK-2026-08-25-REF-02 — triagem dos 19 achados mypy (W-005)

STATUS: DONE (2026-08-25) - 19 achados triados a zero; candidato a bug provado inalcancavel; hashes de malha identicos
Autorização: Felipe Nantes, 2026-08-25 ("o foco deve ser puramente o refactor
agora" — próxima do PURE_REFACTOR_BACKLOG após REF-01). Executor: Fable 5 ·
Effort: MEDIUM · Trilha: PURE_REFACTOR · Human gate: none (zero mudança de
comportamento; tolerâncias do item 9 respeitadas por construção).

## Triagem (19 achados em 4 arquivos → veredito por grupo)

| grupo | achados | veredito | tratamento |
|---|---|---|---|
| stubs ausentes (scipy, totalsegmentator, SimpleITK) | 4 | ambiente, não código | override `[tool.mypy]` no pyproject (só terceiros; código próprio segue checado) |
| yaml sem stub | 1 | ambiente | `types-PyYAML` no [dev] — checagem GENUÍNA em vez de silêncio |
| `stages.py:241` sigma kwarg | 1 | stub swig estreito | chamada posicional; **sonda: saída bitwise idêntica** entre as duas formas |
| `stages.py` 5× `**extra` → arg int | 5 | artefato de inferência (dict misto vira `dict[str, float]`; runtime já era int) | kwargs explícitos nos 5 sítios — mypy verifica cada argumento de verdade |
| `stages.py:1091/1101/1114` MultiBlock vs PolyData (**candidato a bug da auditoria**) | 3 | **INALCANÇÁVEL**: `.vtp` mapeia sempre para vtkXMLPolyDataReader (PolyData) e o arquivo é escrito pelo próprio estágio 6 a partir de PolyData | `cast` documentado (apagado em runtime) |
| `viewer_artifacts.py:287-289/319` união None | 4 | seguro em runtime (`candidate_present` implica not-None), mas o mypy não acompanha a variável booleana | narrowing estrutural no `if` + conjunto tautológico no frame (valor idêntico para todo estado) |
| `volumetry.py:479` indexação em object | 1 | artefato de inferência do dict heterogêneo | dict local `artifacts: dict[str, str]` — MESMO objeto, agora verificado |
| `splits.py:61` (transitivo, exposto pelos stubs novos) | 1 | anotação ausente | `list[Counter[str]]` — anotação pura |

**Veredito do candidato a CORRECTNESS_BUG (stages 1091+)**: NÃO é bug — provado
inalcançável pela invariante extensão→reader; nenhum caminho entrega MultiBlock.

## Oracle / verificação

- mypy nos 5 arquivos tocados: **0 achados** (era 19).
- Gates da CI reproduzidos: ruff defeito+mecânico limpos; gate mypy
  robustness inalterado (o achado transitivo de splits.py não casa com o
  filtro `robustness.py:` e foi corrigido de toda forma).
- **Sonda geométrica (oráculo do backlog)**: `_mesh_from_mask` em esfera
  sintética, DOIS caminhos (legado e campo contínuo com gaussiana+decimação),
  antes (via git stash) vs depois: hashes sha256 de pontos+faces
  **bitwise idênticos** (33e8908e…/1015f1e1…).
- Sonda SimpleITK: `SmoothingRecursiveGaussian(img, sigma=2.0)` vs
  `(img, 2.0)`: arrays bitwise idênticos.
- Testes dirigidos (stages/mesh/volumetry/viewer/splits/engine): 130 passed.
- Portão suíte completa: **1778 passed / 4 skipped / 0 failed** (2m58s).
