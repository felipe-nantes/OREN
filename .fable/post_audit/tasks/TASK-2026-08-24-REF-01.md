# TASK-2026-08-24-REF-01 — consolidação dos writers atomic (W-003/TD-007-duplicação)

STATUS: DONE (2026-08-24) - 11 vazamentos corrigidos + 1 duplicata migrada + guarda estrutural na suite
Autorização: Felipe Nantes, 2026-08-24 ("commit e push, depois siga para a
REF-01"). Rank 8 do TOP_10. Executor: Fable 5 · Effort: MEDIUM ·
Trilha: PURE_REFACTOR + robustez de caminho de falha (classe TD-007).

## Redesenho declarado (proporcional ao risco real)

O censo atual achou **65** funções "atomic" (PH04 contava 56). ~30 vivem em
scripts one-shot congelados — migrá-los seria churn sem valor. Como a PH08
já unificou o COMPORTAMENTO dos canônicos, o risco remanescente do W-003 é
"nova divergência futura". O desenho ataca exatamente isso:

1. **Correção dos 11 vazamentos reais encontrados no censo** (tmp órfão em
   falha — mesma classe do TD-007; padrão try/finally idêntico ao aprovado
   na PH08). Inclui 1 sítio de PRODUÇÃO (`viewer_artifacts._save_png_atomic`)
   e o checkpoint do segmentation_pilot (validações anti-NUL preservadas;
   só exceções inesperadas ganham cleanup).
2. **Migração demonstrativa**: `exam_to_panels._atomic_write_json` (cópia
   byte-idêntica em produção) agora delega ao canônico
   `protocol.atomic_write_json`; imports mortos removidos.
3. **Guarda estrutural permanente**:
   `tests/test_atomic_writers_structural_guard.py` — varre por AST todo
   writer "atomic" do repo (matcher corrige o falso positivo "anatomically")
   e exige try/finally-cleanup OU delegação ao canônico; também pina que o
   canônico do SW-ATOMIC-01 existe com mkstemp+fsync+replace+finally.
   A auditoria da PH04 virou invariante de CI.

## Oracle / verificação

- py_compile dos 10 arquivos tocados; ruff limpo no migrado.
- Guarda + property tests da PH04 + reporting: 17 passed.
- Suíte completa como portão (resultado no fechamento).
- Comportamento do caminho feliz: INALTERADO em todos os sítios (try/finally
  só age em falha; unlink pós-replace é no-op).

## Portão (OBSERVED, 2026-08-24)

Suíte completa (2m49s): **1777 passed / 4 skipped / 0 failed** — 100% verde,
+2 testes de guarda, zero regressões nos 12 arquivos tocados.

STATUS FINAL: DONE (2026-08-24).
