# TASK-2026-08-25-REF-03 — decomposição de webapp/server.py por seams (W-001/TD-001)

STATUS: IN_PROGRESS (2026-08-25) — design + guard + seams 1-3 DONE; seam 4 pendente
Autorização: Felipe Nantes, 2026-08-25 ("commit e push, depois siga para a
REF-03"). Executor: Fable 5 · Effort: alto no design, MEDIUM por seam ·
Trilha: PURE_REFACTOR (byte-idêntico) · Human gate: none (rollback = revert
do seam).

## Fase 1 — DESIGN (concluída)

`.fable/post_audit/plans/REF-03_SERVER_DECOMPOSITION_DESIGN.md`. Achados que
mandam no desenho (medidos): 10 consumidores externos importam de
webapp.server; **69 monkeypatches** sobre ~25 símbolos (WORKSPACE 27×,
process_job, _segment, ...). Regras: **R1** rotas+config+estado ficam em
server.py; **R2** módulo extraído resolve config/estado/patch-targets via
`server.<nome>` em tempo de chamada (late binding — patches continuam
eficazes por construção; ciclo de import seguro).

## Fase 2 — guard pré-corte (concluída)

`tests/test_server_route_inventory.py`: pina as 19 rotas /api (método+path)
e os 31 símbolos do contrato da façade (patch-points + imports de tools).
Verde ANTES do primeiro corte.

## Fase 3 — Seam 1 (concluído): payloads + persistência de job + XR/Quest

- `webapp/payloads.py`: 8 modelos Pydantic (byte-idênticos).
- `webapp/job_persistence.py`: `_case_dir_for_job`,
  `_completed_job_state_path`, `_persist_completed_job_state` (TD-015 retry
  preservado), `_legacy_completed_job_from_artifacts`,
  `_restore_completed_job` — estado `_jobs`/`_lock` e WORKSPACE/DISCLAIMER
  via `server.` (R2).
- `webapp/xr_sessions.py`: `_xr_session_path`, `_read_xr_session`,
  `_quest_base_url`, `_quest_qr_data_url`, `_recent_quest_jobs`.
- server.py: bloco substituído por façade de re-imports (4 re-exports puros
  com noqa documentado); stdlib órfão removido. 3.635 → ~3.290 linhas.

Verificação do seam 1:
- Bateria webapp completa (test_webapp 85 + inventário + boundary + timing
  ×2 + presets + subtype): **131 passed** — todos os 69 monkeypatches
  eficazes através da façade (o risco nº 1 do corte, provado resolvido).
- Gates ruff da CI: limpos nos 4 arquivos.
- Portão suíte completa do seam 1: **1804 passed / 4 skipped / 0 failed**
  (2m12s; +2 do guard de inventário).

## Fase 4 — Seam 2 (concluído): `webapp/benchmarks.py`

Subsistema benchmark inteiro (18 funções, ~800 linhas) extraído; server.py
2.512 → ~1.750 linhas (de 3.635). Duas lições incorporadas ao método:
1. **Chamada interna a patch-target também é superfície de patch** — a 1ª
   rodada quebrou exatamente o teste previsto (dual_report): process_benchmark
   chamava _run_visual_benchmark_case pela global local. Corrigido: TODA
   chamada intra-módulo a patch-target passa por `server.` (10 call sites).
2. **O inventário de patch-targets exigia varredura TOTAL** — o guard da
   GOV-01 (test_estimand_provenance_negative) patcha _visual_bundle_root e
   load_screening_config (que chega por IMPORT no server). Varredura completa
   de setattr(server,...) em tests/+tools/: 26 alvos; guard de inventário
   atualizado com os 5 que faltavam.
Constantes SUBTYPE_LABELS_PT/SCREENING_TARGET_SUBTYPE moradoras do meio do
bloco voltaram ao server (R1). Façades convertidas para alias redundante
PEP 484 (`X as X`) — estável sob F401/RUF100 sem noqa.
Portão seam 2: bateria webapp 131 passed + estimand guard; suíte completa
**1804 passed / 4 skipped / 0 failed** (2m08s).

## Fase 5 — Seam 3 (concluído): `webapp/jobs.py`

Os 4 workers (process_job, process_visual_job, process_monophase_medsiglip_job,
advisory tardio; 2 blocos, ~800 linhas) extraídos; server.py → ~1.790 linhas.
Endurecimentos desta rodada:
1. **Varredura multiline-aware**: `select_best_mr_series` era patchado em
   chamada setattr MULTILINHA que o grep de uma linha não via — inventário
   REAL: 30 alvos (regex DOTALL); guard atualizado.
2. **`import time` é superfície de patch**: o teste de boundary patcha
   `server.time.sleep`; import restaurado com noqa documentado.
3. Checagem SISTEMÁTICA de usos não-prefixados dos 30 alvos nos 5 módulos
   extraídos: pegou 3 chamadas internas de `_case_dir_for_job` no seam 1
   contornando silenciosamente o patch (corrigidas) + 2 de
   select_best_mr_series.
4. Teste de INSPEÇÃO DE FONTE (shadow_3d) atualizado: o literal agora carrega
   o prefixo `server.` da façade; invariante (ordem shadow/malha + gating)
   idêntico — única adaptação de teste em 3 seams, documentada.
Portão seam 3: bateria 136 verde; suíte **1804 passed / 4 skipped / 0 failed**.

## Pendente (ordem do design)

- Seam 4: `webapp/phase_union.py` (união de fases — downstream científico,
  byte-idêntico, specs de geometria intactas).
