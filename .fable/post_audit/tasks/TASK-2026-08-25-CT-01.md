# TASK-2026-08-25-CT-01 — ingestão de TC no fluxo completo do OREN (fases A–E)

STATUS: DONE (2026-08-25) - CT atras de flag, sem triagem, fluxo MR byte-identico; fase F (validacao local) pendente e gated
Autorização: Felipe Nantes, 2026-08-25 (plano CT-01 aprovado via plan mode;
decisões do operador: flag WEBAPP_CT_ENABLED + seleção explícita de
modalidade na UI). Insumo: OREN_CT_INGESTAO_ESPECIFICACAO.md (extração do
Volyrics, docs/249-250 daquele repo). Plano:
`.fable/post_audit/plans/CT-01_INGESTAO_CT_PLAN.md`.

## Entregas (fases A–E do plano)

- **A** `profiles/figado_ct.yaml`: modalidade [CT], task `total`, Couinaud
  `liver_segments` fail-closed, candidato DESABILITADO (task CT sem nome
  confirmado), `validado: false`, mesh herdado com comentário honesto.
  figado.yaml (RM) INTOCADO.
- **B** server.py: `MODALITY_PROFILES` + `CT_ENABLED` (env) +
  `_profile_path_for` + campo `modality` no /api/analyze (default MR =
  compat total; CT exige flag; cenário explícito e 3-D aprimorado recusados
  em TC) + `ct_enabled` no /api/health. `_segment` e `_build_model` ganham
  kwarg opcional `profile_rel` (default preserva MR; nenhum caller de RM
  mudou). UI: seletor MR/CT visível só com a flag; em TC não envia scenario
  nem backend nem enhanced_3d.
- **C** `webapp/jobs.py`: `_select_ct_series` (série única por
  SeriesInstanceUID, só Modality==CT — D3) + `process_ct_job` (segmentação
  GPU→CPU com perfil CT → malha/volumetria → aprovação; SEM triagem — D4;
  payload com `screening_available: false` + motivo + volume do manifesto +
  nota de calibração D5 com proveniência Volyrics e
  `correcao_aplicada: False`).
- **D** `_aviso_volumetria_ct()` — nota consultiva; faixa 900-2400 mL de RM
  NÃO reaproveitada (W-038).
- **E** `tests/test_ct_ingestion.py` (14 testes): perfil, mapeamento, flag,
  rota (409/400/despacho), série CT sintética, mismatch gracioso e o TESTE
  NEGATIVO central — sentinelas `pytest.fail` em TODA a superfície de
  triagem (_run, load_screening_config, process_job, monofásico, advisory)
  provam que um job CT nunca a toca. Guard da façade ganhou os 5 símbolos
  novos.

## Verificação

- 14/14 testes CT verdes (de primeira, sem retry).
- Bateria MR completa (webapp 136): **byte-idêntica**, verde.
- Portão suíte completa: **1823 passed / 4 skipped / 0 failed** (2m28s;
  1809 + 14).
- Gates ruff da CI limpos nos arquivos tocados; guards de rotas/configs/
  tools verdes (rota /api/analyze com assinatura inalterada; campo aditivo).

## Governança (conforme plano)

Nenhum contrato científico MR tocado (perfil, bundles, thresholds, folds,
resolver intocados — a bateria MR é o oráculo). stages.py INTOCADO (W-002
segue gated). HG-05 não disparou. HG-12 protegido: TC sem triagem, PESQUISA,
revisão humana obrigatória, números externos citados com proveniência.

## Pendente (fase F — GATED, não bloqueia o uso com flag)

Benchmark volumétrico LOCAL de TC (CHAOS-CT/3D-IRCADb; presença de dados a
confirmar — candidatos no D:) + amostra Couinaud-CT. Só então
`validado: true` e eventual remoção da flag. Follow-ups documentados:
recalibração de mesh p/ espaçamento CT; windowing HU p/ inspeção 2D.
