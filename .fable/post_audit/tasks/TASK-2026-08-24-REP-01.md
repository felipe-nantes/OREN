# TASK-2026-08-24-REP-01 — sonda de tolerâncias GPU/CUDA (W-016, fecha BLK-GPU-TOLERANCES)

STATUS: DONE (2026-08-24) - tolerancia ratificada (item 20); BLK-GPU-TOLERANCES fechado
Autorização: Felipe Nantes, 2026-08-24 ("commit e push, depois siga para a
REP-01"). Rank 6 do TOP_10. Executor: Fable 5 · Effort: HIGH ·
Tipo: MEDIÇÃO (nenhum número de produção muda); a RATIFICAÇÃO da tolerância
proposta estende a decisão 9 (gate humano no fechamento).

## Contexto

HUMAN_DECISIONS item 9 ratificou tolerâncias CPU (LOGIC exato; escalar
rel≤1e-12; array zero divergência) e deixou GPU/CUDA explicitamente EM
ABERTO. A RTX 4060 desta máquina permite fechar o blocker agora
(torch 2.6.0+cu124, cudnn 90100 — o MESMO stack do container canônico).

## Desenho (em camadas)

- **Tier 1 — famílias de ops reais do sistema** (conv3d/interpolate ≈
  segmentação; matmul/softmax/layernorm ≈ embeddings; reduções):
  1. run-to-run GPU com flags determinísticas LIGADAS → bitwise?
  2. run-to-run GPU em modo PADRÃO de produção (flags default) → bitwise?
  3. GPU vs CPU (float32) → máx |Δ| absoluto e relativo por op.
  Seeds fixas; 3 repetições por medição; tamanhos representativos.
- **Tier 2 — superfície real (MedSigLIP)** SOMENTE se os pesos já estiverem
  no cache local (sem downloads): forward 2× GPU + 1× CPU numa imagem
  sintética fixa; comparação do embedding 1152-d.
- Proposta de tolerância GPU para ratificação, no formato da decisão 9.

## Proibições

- Nenhum download de modelo; nenhum número de produção recomputado; nenhum
  artefato congelado tocado; não commitar/pushar sem pedido.

## Critérios de saída

- Tabela de deltas por op/regime (evidence/REP-01/); proposta de tolerância
  apresentada no gate; blocker fechado SE ratificado.
