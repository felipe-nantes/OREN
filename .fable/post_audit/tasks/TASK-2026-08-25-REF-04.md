# TASK-2026-08-25-REF-04 — naming canônico ARGOS/OREN/Volyrcs (W-006/TD-013)

STATUS: DONE (2026-08-25) - secao canonica no README + ponteiro no CLAUDE.md; diff so em .md
Autorização: Felipe Nantes, 2026-08-25 ("commit e push, depois siga para a
REF-04"). Executor: Fable 5 · Effort: LOW · Trilha: PURE_REFACTOR (docs) ·
Human gate: none. A metade W-007 do REF-04 original já foi fechada na DOC-01.

## Diagnóstico (OBSERVED)

Os três nomes têm referentes DISTINTOS e coerentes — o drift era nenhuma doc
viva dizer isso:
- **ARGOS**: título do README, manuscrito, pipeline (`dtwin/`) — o projeto
  de pesquisa.
- **OREN**: o que o usuário VÊ — `<title>` e wordmark do webapp, footer,
  launchers `INICIAR_OREN*.cmd`, e o próprio repo GitHub (OREN.git).
- **Volyrcs**: produto em desenho (docs/230, volumetria com aprovação humana
  obrigatória); ZERO ocorrências no runtime (só docs/230 e graphify-out).

Decisão de escopo: NÃO renomear nada (renomear marca/arquivos é decisão de
produto, não refactor) — tornar a coexistência CANÔNICA e documentada.

## Mudanças (diff só em .md — oráculo do backlog)

1. `README.md`: nova seção "Os três nomes (canônico)" após o aviso de Modo
   Pesquisa — define cada nome, seu escopo, e que docs históricas usam o
   nome da época.
2. `CLAUDE.md` (§PROJECT): uma linha com o resumo do naming canônico
   apontando para a seção do README — agentes resolvem o naming na entrada.

## Verificação

- `git diff --stat`: somente CLAUDE.md (+1) e README.md (+15) — zero código,
  zero configs, zero testes; suíte dispensada pelo próprio backlog
  (behavioral_oracle: "diff só em .md"; tests: n/a).
