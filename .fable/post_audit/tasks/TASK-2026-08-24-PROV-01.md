# TASK-2026-08-24-PROV-01 — mapa de proveniência dos E-IDs do manuscrito (W-034/TD-012)

STATUS: DONE (2026-08-24) - PROVENANCE_MAP.yaml com 28/28 docs+commits; lacunas de comando/artefato quantificadas
Autorização: Felipe Nantes, 2026-08-24 ("commit e push, depois siga para a
PROV-01"). Rank 10 do TOP_10. Executor: Fable 5 · Effort: MEDIUM ·
Tipo: PROVENANCE (nenhum código, teste, contrato ou número tocado).

## Descoberta central (OBSERVED)

**Os E-IDs do manuscrito são a numeração de `docs/` sem zero à esquerda**
(E016 → `docs/16_*.md`). Fonte dos IDs: tabela "Rastreabilidade das alegações
centrais" (C1–C9) nas pp. 32–33 do `Manuscrito_Integrado_v1_ARGOS_OREN.pdf`
(v1, 2026-08-09 — o mesmo SOURCE_B da reconciliação MVC). A seção é marcada
"Notas editoriais internas — remover antes da submissão", ou seja, os E-IDs
são controle interno dos autores, não parte do argumento científico.

Correção de premissa do TD-012: "E016…E154" descreve a FAIXA da numeração,
não a contagem. Expandindo as faixas citadas (E094–E098, E145–E147,
E148–E154), o manuscrito cita **28 E-IDs distintos** — todos resolvidos.
Nenhum outro doc do repo nem o zip docs/186 contém E-IDs (verificado por
grep); o PDF em Downloads é a única fonte.

## Método

1. Texto extraído do PDF com pypdf instalado ISOLADO no scratchpad
   (`--target`; o venv do projeto não foi tocado).
2. Cada E-ID → `docs/<n>_*.md` (glob; 28/28 únicos e existentes).
3. Por doc: primeiro commit (`git log --reverse`), título, comandos literais
   (regex python/tools/dtwin.*) e artefatos citados (casos/configs/data/...).
4. Existência de cada artefato verificada em DOIS lugares: checkout local
   (`existe_local`) e espelho do SSD `D:/OREN/argos-main` (`existe_ssd`).
   Nenhum label protegido lido; só existência de caminho.

## Resultado (evidence/PROV-01/PROVENANCE_MAP.yaml)

| dimensão | cobertura |
|---|---|
| E-ID → doc | **28/28** |
| E-ID → primeiro commit | **28/28** |
| E-ID → comando literal no doc | 5/28 |
| E-ID → artefatos citados no doc | 22/28 |
| E-ID → ≥1 artefato alcançável (local ou SSD) | 13/28 |

- A lacuna comando/artefato NÃO é falha do mapa: os docs são relatórios de
  resultado que raramente registram a linha de comando — exatamente a lacuna
  de ledger do W-033/SR-002, agora QUANTIFICADA por E-ID.
- Toda a árvore `casos/qualification/openswisshcc_v1/` (citada por E063–E065
  etc.) não existe neste checkout nem no SSD → máquina de origem (blocker
  ambiental já documentado). 9 artefatos citados existem no SSD; 37 não.
- C5 e C9 não citam E-IDs (apontam para conceitos/etapas); registrados no
  mapa com nota descritiva.

## Encaminhamento

- W-034 → RESOLVED (o mapa pedido existe, com cobertura declarada).
- O resíduo (comandos não registrados; artefatos apenas na máquina de origem)
  pertence ao W-033 (SR-002/004/005), que permanece OPEN/AWAITING_HUMAN.
- HG-01: nenhuma decisão de escopo precisou de gate — o mapa é leitura pura;
  nada foi movido, alterado ou recomputado.
