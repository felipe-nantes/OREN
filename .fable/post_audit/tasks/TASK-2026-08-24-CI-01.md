# TASK-2026-08-24-CI-01 — gates mecânicos na CI + conserto da coleta (W-013/ROB-04)

STATUS: DONE (2026-08-24) - aplicado; confirmacao do Actions pos-push pendente
Autorização: Felipe Nantes, 2026-08-24 ("commit e push, depois siga para a
CI-01"). Rank 4 do TOP_10. Executor: Fable 5 · Effort: MEDIUM ·
CAN_CHANGE_SCIENTIFIC_RESULT: NO (workflow/packaging/gating de ambiente).

## Achado no caminho (OBSERVED)

A CI instalava `-e .[dev]` SEM hypothesis (property tests da PHASE_04),
SEM scikit-learn (importado top-level pelos classifiers que os testes puxam)
e SEM torch (1 teste com import top-level) → **a coleta da suíte quebrava na
CI, provavelmente vermelha desde 2026-08-18** — exatamente o "estado real não
verificado" que o W-013 registrava. Análise estática dos imports externos
confirmou os 3 ausentes (+ cryptography, só em tool manual, não afeta).

## Mudanças aplicadas

1. `pyproject.toml [dev]` += hypothesis, pytest-cov, coverage, ruff, mypy
   (comentário cita a task); CI instala `.[dev,training]` (training já
   tinha scikit-learn/joblib — taxonomia existente respeitada).
2. `tests/test_medgemma_volume_score_server.py`: `pytest.importorskip`
   para torch/transformers ANTES do import do server — arquivo pula em
   ambiente sem GPU-stack (deliberado na CI) e roda integralmente onde
   torch existe (verificado: 5 passed no host). Sem enfraquecimento.
3. `.github/workflows/tests.yml` — 4 gates novos:
   - ruff regras de defeito real (F821,F811,F702,B002,B006,B008,B015,
     B023,B031,B032,PLE) — zerado nas fases 07-08;
   - ruff regras mecânicas (I001,RUF100,UP035,F401) com per-file-ignores
     para os 2 residuais justificados da PHASE_08;
   - mypy do módulo zerado (robustness.py) via filtro;
   - suíte COM cobertura + piso do núcleo científico >= 85% branch
     (auditoria mediu 89,5%; margem conservadora; os 8 módulos do núcleo
     não importam torch — piso estável na CI sem GPU).

## Verificação

- 4 comandos de gate executados localmente idênticos aos da CI: todos
  verdes; YAML/TOML parse OK; teste guardado roda com torch local.
- Suíte completa com cobertura + comando exato do floor: portão em
  background (resultado no evidence).
- Verificação DEFINITIVA (Actions verde) só é observável após o push —
  registrada como passo de fechamento com o operador.

## Portão (OBSERVED, 2026-08-24)

- Suíte completa com cobertura (3m13s): **1774 passed / 4 skipped /
  1 failed ambiental** (test_environment_report_accepts_free_gpu, GPU
  ocupada — ruído conhecido; zero regressões das mudanças).
- Comando EXATO do floor da CI: núcleo **89,5% ≥ 85** → exit 0.
- Nota: volumetry.py está em 83,7% individual — o floor é sobre o TOTAL do
  núcleo (89,5%), como especificado; registrado para não surpreender.

## Fechamento pendente

A prova definitiva (Actions verde no GitHub) só é observável após o push —
pedir ao operador para conferir a aba Actions do repositório no primeiro
push depois desta task.

STATUS FINAL: DONE (2026-08-24) — aplicado e verificado localmente.
