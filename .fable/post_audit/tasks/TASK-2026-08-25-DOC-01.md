# TASK-2026-08-25-DOC-01 — reconciliação de docs: resolver DICOM e escopo do README (W-039/W-007)

STATUS: DONE (2026-08-25) - 3 docs reconciliadas; W-007 RESOLVED; W-039 metade documental fechada
Autorização: Felipe Nantes, 2026-08-25 ("commit e push, depois siga para a
DOC-01"). Executor: Fable 5 · Effort: LOW · Trilha: docs (SR-009/TD-006) ·
Human gate: none (nenhuma semântica de código muda; docstring é texto).

## Escopo declarado

Só documentação. O segundo item do W-039 ("agregador dos campos de auditoria
no braço de benchmark") é CÓDIGO do benchmark arm e fica explicitamente
ADIADO — W-039 permanece OPEN com nota; esta task fecha a metade documental
e o W-007 inteiro.

## Mudanças

1. **README.md — "Fora do escopo por enquanto" (W-007/TD-006)**: Couinaud e
   vasculatura hepática saíram da lista de fora-de-escopo (contradiziam
   `profiles/figado.yaml`, que os habilita em `segmentacao_anatomia`).
   Redação nova sem overclaim: camadas OPCIONAIS de visualização 3D,
   fail-closed (Couinaud exige I–VIII válidos; ausência não invalida máscara
   nem triagem), sem validação anatômica externa, citando docs/191–193 —
   que reprovaram o modelo dedicado como SUBSTITUTO do segmentador de órgão
   (papel diferente do uso no viewer; nuance preservada).
2. **docs/123 §3 (SR-009)**: bloco datado "> Atualização (2026-08-25)" APÓS o
   texto histórico (que afirmava "não existe identificação automática de
   fase" e a listava como passo futuro): o resolver existe
   (`raw_dicom_phase_resolver.py`), é fallback fail-closed de
   `build_multiphase_case`, pastas nomeadas continuam soberanas, ambiguidade
   aborta, manifesto auditável com `series_with_ambiguous_text_roles` e
   `unselected_eligible_dynamic_series`, validação em
   `tools/run_raw_phase_equivalence_benchmark.py`. Texto original preservado
   como registro histórico.
3. **`dtwin/learning/multiphase_ingest.py` — docstring do módulo (SR-009)**:
   removida a afirmação "automatic identification ... is an unsolved ...
   problem (out of scope)"; descreve os dois modos reais (pastas nomeadas
   soberanas; fallback fail-closed) e mantém o limite honesto: timing de
   contraste segue sem confirmação clínica, resolver heurístico
   research-only.

## Verificação

- Ruff limpo no .py tocado (mudança é só docstring; zero semântica).
- Testes dirigidos de multiphase/resolver: 61 passed; gates ruff da CI
  reproduzidos localmente no arquivo: limpos.
- Portão suíte completa: **1778 passed / 4 skipped / 0 failed** (2m30s).
