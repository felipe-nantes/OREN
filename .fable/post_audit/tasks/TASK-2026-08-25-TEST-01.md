# TASK-2026-08-25-TEST-01 — determinismo da suíte: teste ambiental + perfil Hypothesis (W-011/W-012)

STATUS: DONE (2026-08-25) - suite deterministica; portao 1778/4/0 COM o disco real abaixo do limiar
Autorização: Felipe Nantes, 2026-08-25 ("volte para o refactor" — próxima do
TOP_10). Rank 9. Executor: Fable 5 · Effort: LOW · Trilha: ROBUSTNESS_FIX ·
Human gate: none (test-only; nenhum código de produção tocado).

## Descoberta (OBSERVED) — o diagnóstico da auditoria estava incompleto

W-011 registrava "test_environment_report_accepts_free_gpu falha com GPU
ocupada". O teste **mocka `query_gpu` e `module_versions`** — a GPU real não
entra. O vazamento ambiental verdadeiro é `shutil.disk_usage`, NÃO mockado:
`build_environment_report` mede o disco REAL, e com C: em ~20 GiB livres
(limiar `DISK_MINIMUM_FREE_GIB = 20.0`) o teste falha AGORA de forma
determinística por `insufficient_disk_space`. A "falha ambiental de GPU" dos
portões da auditoria era, pelo menos em parte, o disco da máquina roçando o
limiar. Dois vazamentos, mesmo teste; hoje quem dispara é o disco.

## Mudanças (test-only)

1. `tests/test_learning_environment.py`:
   - `_mock_disk()` compartilhado; os 3 testes existentes agora mockam o
     disco (400 GiB livres) — determinísticos em qualquer máquina.
   - NOVO `test_environment_report_blocks_insufficient_disk`: primeira
     cobertura determinística do blocker de disco (free = limiar − 1 GiB;
     `blockers == ["insufficient_disk_space"]`; `require_training_ready`
     levanta). Proteção AUMENTA, não enfraquece: produção continua medindo o
     disco real; só o teste unitário deixou de depender da máquina.
2. `tests/conftest.py` (W-012): perfil Hypothesis `argos_suite`
   (`deadline=None` + `suppress_health_check=[HealthCheck.too_slow]`).
   O flake da PH09 (`nenhum_grupo_...`, 1 ocorrência, nunca reproduzido em 3
   reexecuções) com `deadline=None` já presente em TODOS os @given aponta
   para o health check `too_slow` na geração de exemplos sob carga
   (INFERRED — traceback original não preservado). O perfil suprime apenas
   esse guard de tempo; max_examples e asserções intactos. Decorators
   explícitos continuam prevalecendo sobre o perfil.

## Oracle / verificação

- Testes dirigidos: 11 passed (5 environment incl. novo + 6 splits isolation).
- Ruff limpo nos 2 arquivos.
- Suíte completa como portão: **1778 passed / 4 skipped / 0 failed** (3m48s),
  executada COM o disco real abaixo do limiar — a condição que antes produzia
  o falso-vermelho agora passa deterministicamente. Zero regressões.
